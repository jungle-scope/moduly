# Helm Ignore File
# Patterns to ignore when building Helm chart package

# Development files
*.swp
*.bak
*.tmp
*.orig
*~
.DS_Store

# IDE and editor files
.vscode/
.idea/
*.iml
.project
.classpath
.settings/

# Git files
.git/
.gitignore
.gitattributes
.gitmodules

# CI/CD files
.github/
.gitlab-ci.yml
.travis.yml
Jenkinsfile

# Documentation (optional - keep if you want docs in package)
# README.md
# NOTES.txt
*.md

# Test files
test/
tests/
*_test.yaml
*_test.yml

# Local development files
.env
.env.local
*.local

# Backup files
*.backup
backup/

# OS files
Thumbs.db
desktop.ini

# Helm chart dependencies (these are fetched during install)
charts/*.tgz
charts/*/

# Lock files
requirements.lock
Chart.lock.bak

# Python
__pycache__/
*.py[cod]
*$py.class
venv/
.venv/
env/
pip-log.txt

# Node.js / Next.js
node_modules/
.next/
dist/
build/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Docker
Dockerfile*
docker-compose*.yml
.dockerignore

# Terraform (infra 폴더 안에 같이 있는 경우)
*.tf
*.tfstate
*.tfstate.backup
.terraform/

# Keys and Secrets
*.pem
*.crt
*.key
*.p12
*.jks
id_rsa
id_rsa.pub

# 민감한 설정 파일 (실수 방지)
# values.yaml은 필수지만, 운영용/비밀용 파일은 제외
values-prod.yaml
values-secret.yaml
secrets.yaml

# Logs
*.log
